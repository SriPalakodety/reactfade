import React from "react";
import ReactDOM from "react-dom";

import {ScrollExample} from "./ScrollExample";

const rootElement = document.getElementById("root");
ReactDOM.render(<ScrollExample />, rootElement);
